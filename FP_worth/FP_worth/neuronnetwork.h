#ifndef NEURONNETWORK_H
#define NEURONNETWORK_H
/*
#include "fptree.h"
#include "fpworth.h"

#define and((a),(b)) ((a)<(b)?(a):(b))
#define or((a),(b)) ((a)>(b)?(a):(b))

//typedef struct mfunc struct membershipFunction;

typedef struct{
    int number;
    double support;
} term;

class NeuronNetwork
{
public:
    NeuronNetwork(QVector< QVector<struct membershipFunction> > mfuncs,
                  QVector<double> input, QVector<FPTree*> trees);
    double evaluate();
private:
    QVector< QVector<struct membershipFunction> > membershipFunctions;
    QVector<FPTree*> fpTrees;
    QVector<double> alphas;
    QVector<double> betas;
    QVector<struct term> fuzzyInput;
    double f;


    void layout1();
    void layout2();
    void layout3();
    void layout4();
};
*/
#endif // NEURONNETWORK_H


